import { RegisterForm } from "@/components/auth/register-form";

export default function RegisterPacientePage() {
  return (
    <RegisterForm
      role="paciente"
      roleLabel="Paciente"
    />
  );
}
