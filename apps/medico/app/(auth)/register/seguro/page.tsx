import { RegisterForm } from "@/components/auth/register-form";

export default function RegisterSeguroPage() {
  return (
    <RegisterForm
      role="seguro"
      roleLabel="Seguro"
    />
  );
}
